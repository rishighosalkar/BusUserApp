<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bususerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusUserApp">
        <activity android:name=".Activities.LostFoundMainActivity"></activity>
        <activity android:name=".Activities.PostActivity" />
        <activity android:name=".Activities.PostViewActivity" />
        <activity android:name=".Activities.ProfileViewActivity" />
        <activity android:name=".Activities.MessageActivity" />
        <activity android:name=".Activities.MessageViewActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>